apiVersion: v1
kind: Pod
metadata:
  name: test
  labels:
    env: prod
spec:
  containers:
  - name: podtest
    image: nginx:now
    ports:
    - containerPort: 80

---

kind: Service
apiVersion: v1
metadata:
  name: my-service
spec:
  selector:
    env: prod
  ports:
  - protocol: TCP
    targetPort: 80

